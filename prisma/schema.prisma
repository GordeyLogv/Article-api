generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model ArticleModel {
  id Int @id @default(autoincrement())
  title String
  content String
  imageUrl String
  views Int
  likes Int
  createdAt DateTime
}